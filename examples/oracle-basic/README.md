# Oracle Basic Example

This example demonstrates constructing `OracleSink` directly from Python. The companion
`config.json` file shows the equivalent JSON runtime configuration shape.

```bash
export ORACLE_PASSWORD=example
python examples/oracle-basic/consumer_demo.py
```

The script is intentionally small and is meant for local development only.
